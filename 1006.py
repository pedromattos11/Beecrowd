# Entrada de dados
a = float(input())
b = float(input())
c = float(input())

# Cálculo da média ponderada
media_ponderada = (a * 2 + b * 3 + c * 5) / 10

# Saída do resultado usando format // versões diferentes
print('MEDIA = {:.1f}'.format(media_ponderada))