# Leia os dois valores inteiros
A <- as.integer(readline(prompt = "Digite o valor de A: "))
B <- as.integer(readline(prompt = "Digite o valor de B: "))

# Calcule a soma
X <- A + B

# Imprima o resultado formatado
cat("X = ", X, "\n")